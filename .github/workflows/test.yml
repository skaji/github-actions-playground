name: test

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: skaji/action-generate-tarball@v1
        with:
          top-directory: foo
          exclude: |
            ^\\.git/
            ^test.sh$
      - run: tar tf foo.tar.gz
