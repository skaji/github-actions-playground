name: test

on:
  push:
    tags:
      - '*'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          gh release create ${{ github.ref }} cpanfile
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
