name: test

on:
  - push

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: perl:latest
      options:
        - --tty
    steps:
      - uses: actions/checkout@v2
      - run: cpanm -nq Capture::Tiny
      - run: cpanm
        shell: bash
      - run: cpanm --unknoooooo
        shell: bash
      - run: perl test.pl
        shell: bash
