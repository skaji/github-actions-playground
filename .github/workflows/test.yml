name: test

on:
  push:
    branches:
      - '*'
    tags-ignore:
      - '*'
  pull_request:

jobs:
  perl:
    runs-on: ubuntu-latest
    steps:
      - run: docker run -d -p 8080:80 kennethreitz/httpbin
      - run: curl http://localhost:8080/ip
